'use client';\n\nimport React from 'react';\nimport { FaWhatsapp, FaShare } from 'react-icons/fa';\n\ninterface WhatsAppStatusButtonProps {\n  content: string;\n  image?: string;\n  onShare: () => void;\n  isLoading?: boolean;\n}\n\nexport const WhatsAppStatusButton: React.FC<WhatsAppStatusButtonProps> = ({\n  content,\n  image,\n  onShare,\n  isLoading = false,\n}) => {\n  const handleShare = async () => {\n    try {\n      // Simular API call para enviar para WhatsApp Status\n      await new Promise((resolve) => setTimeout(resolve, 1000));\n      onShare();\n    } catch (error) {\n      console.error('Erro ao compartilhar no WhatsApp Status:', error);\n    }\n  };\n\n  return (\n    <button\n      onClick={handleShare}\n      disabled={isLoading}\n      className=\"flex items-center justify-center gap-2 bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg font-medium transition disabled:opacity-50 disabled:cursor-not-allowed\"\n      title=\"Compartilhar no Status do WhatsApp\"\n    >\n      <FaWhatsapp size={18} />\n      <span>{isLoading ? 'Enviando...' : 'Status WhatsApp'}</span>\n      <FaShare size={14} />\n    </button>\n  );\n};\n\ninterface WhatsAppStatusModalProps {\n  isOpen: boolean;\n  onClose: () => void;\n  content: string;\n  image?: string;\n  onConfirm: () => void;\n  isLoading?: boolean;\n}\n\nexport const WhatsAppStatusModal: React.FC<WhatsAppStatusModalProps> = ({\n  isOpen,\n  onClose,\n  content,\n  image,\n  onConfirm,\n  isLoading = false,\n}) => {\n  if (!isOpen) return null;\n\n  return (\n    <div className=\"fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50\">\n      <div className=\"bg-white rounded-lg shadow-xl p-6 max-w-md w-full mx-4\">\n        <div className=\"flex items-center gap-2 mb-4\">\n          <FaWhatsapp className=\"text-green-500 text-2xl\" />\n          <h2 className=\"text-2xl font-bold text-blackbox-900\">Compartilhar no Status</h2>\n        </div>\n\n        <div className=\"bg-green-50 rounded-lg p-4 mb-4 max-h-48 overflow-y-auto\">\n          {image && (\n            <img src={image} alt=\"Preview\" className=\"w-full rounded-lg mb-3 max-h-32 object-cover\" />\n          )}\n          <p className=\"text-blackbox-900\">{content}</p>\n        </div>\n\n        <p className=\"text-sm text-blackbox-600 mb-4\">\n          Seu status será visível para seus contatos no WhatsApp por 24 horas.\n        </p>\n\n        <div className=\"flex gap-2\">\n          <button\n            onClick={onConfirm}\n            disabled={isLoading}\n            className=\"flex-1 flex items-center justify-center gap-2 bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg font-bold transition disabled:opacity-50 disabled:cursor-not-allowed\"\n          >\n            <FaWhatsapp /> {isLoading ? 'Enviando...' : 'Enviar'}\n          </button>\n          <button\n            onClick={onClose}\n            disabled={isLoading}\n            className=\"flex-1 border border-blackbox-300 text-blackbox-900 px-4 py-2 rounded-lg hover:bg-blackbox-100 font-medium transition\"\n          >\n            Cancelar\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n};\n